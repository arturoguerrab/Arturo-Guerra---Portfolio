import React from "react";

const DashboardContainer = ({ children }) => {
	return <main className="pt-20  pb-[5.5rem] w-full px-2 xl:px-10 max-w-[1920px]">{children}</main>;
};

export default DashboardContainer;
