import React from "react";

const DashboardContainer = ({ children }) => {
	return <main className="pt-20  pb-[4.5rem] w-full max-w-[1920px]">{children}</main>;
};

export default DashboardContainer;
